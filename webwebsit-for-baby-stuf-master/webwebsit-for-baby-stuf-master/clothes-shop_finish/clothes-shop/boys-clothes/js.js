console.log('helooo');

document.getElementById('glavni').style.display='block';
document.getElementById('viewMore').style.display='none';

function povecajSliku(broj){
    if(broj==1)
    {
        document.getElementById('glavni').style.display='none';
        document.getElementById('viewMore').style.display='flex';

        // 1. Kreiranje img elementa
        const imgElement = document.createElement('img');

        // 2. Postavljanje atributa
        imgElement.src = 'card1-photo.webp';
        imgElement.width = 550; // Širina u pikselima
        imgElement.id = 'dodati';

        // 3. Dohvatanje roditeljskog elementa
        const container = document.getElementById('slika');

        // 4. Dodavanje img elementa u DOM
        container.appendChild(imgElement);
    }
    else if(broj==2)
    {
        document.getElementById('glavni').style.display='none';
        document.getElementById('viewMore').style.display='flex';

        // 1. Kreiranje img elementa
        const imgElement = document.createElement('img');

        // 2. Postavljanje atributa
        imgElement.src = 'card2-photo.webp';
        imgElement.width = 550; // Širina u pikselima
        imgElement.id = 'dodati';

        // 3. Dohvatanje roditeljskog elementa
        const container = document.getElementById('slika');

        // 4. Dodavanje img elementa u DOM
        container.appendChild(imgElement);
    }
    else if(broj==3)
    {
        document.getElementById('glavni').style.display='none';
        document.getElementById('viewMore').style.display='flex';

        // 1. Kreiranje img elementa
        const imgElement = document.createElement('img');

        // 2. Postavljanje atributa
        imgElement.src = 'card3-photo.webp';
        imgElement.width = 550; // Širina u pikselima
        imgElement.id = 'dodati';

        // 3. Dohvatanje roditeljskog elementa
        const container = document.getElementById('slika');

        // 4. Dodavanje img elementa u DOM
        container.appendChild(imgElement);
    }
    else if(broj==4)
    {
        document.getElementById('glavni').style.display='none';
        document.getElementById('viewMore').style.display='flex';

        // 1. Kreiranje img elementa
        const imgElement = document.createElement('img');

        // 2. Postavljanje atributa
        imgElement.src = 'card4-photo.webp';
        imgElement.width = 550; // Širina u pikselima
        imgElement.id = 'dodati';

        // 3. Dohvatanje roditeljskog elementa
        const container = document.getElementById('slika');

        // 4. Dodavanje img elementa u DOM
        container.appendChild(imgElement);
    }
    else if(broj==5)
    {
        document.getElementById('glavni').style.display='none';
        document.getElementById('viewMore').style.display='flex';

        // 1. Kreiranje img elementa
        const imgElement = document.createElement('img');

        // 2. Postavljanje atributa
        imgElement.src = 'card5-photo.webp';
        imgElement.width = 550; // Širina u pikselima
        imgElement.id = 'dodati';

        // 3. Dohvatanje roditeljskog elementa
        const container = document.getElementById('slika');

        // 4. Dodavanje img elementa u DOM
        container.appendChild(imgElement);
    }
    else if(broj==6)
    {
        document.getElementById('glavni').style.display='none';
        document.getElementById('viewMore').style.display='flex';

        // 1. Kreiranje img elementa
        const imgElement = document.createElement('img');

        // 2. Postavljanje atributa
        imgElement.src = 'card6-photo.webp';
        imgElement.width = 550; // Širina u pikselima
        imgElement.id = 'dodati';

        // 3. Dohvatanje roditeljskog elementa
        const container = document.getElementById('slika');

        // 4. Dodavanje img elementa u DOM
        container.appendChild(imgElement);
    }
    else if(broj==7)
    {
        document.getElementById('glavni').style.display='none';
        document.getElementById('viewMore').style.display='flex';

        // 1. Kreiranje img elementa
        const imgElement = document.createElement('img');

        // 2. Postavljanje atributa
        imgElement.src = 'card7-photo.webp';
        imgElement.width = 550; // Širina u pikselima
        imgElement.id = 'dodati';

        // 3. Dohvatanje roditeljskog elementa
        const container = document.getElementById('slika');

        // 4. Dodavanje img elementa u DOM
        container.appendChild(imgElement);
    }else if(broj==8)
    {
        document.getElementById('glavni').style.display='none';
        document.getElementById('viewMore').style.display='flex';

        // 1. Kreiranje img elementa
        const imgElement = document.createElement('img');

        // 2. Postavljanje atributa
        imgElement.src = 'card8-photo.webp';
        imgElement.width = 550; // Širina u pikselima
        imgElement.id = 'dodati';

        // 3. Dohvatanje roditeljskog elementa
        const container = document.getElementById('slika');

        // 4. Dodavanje img elementa u DOM
        container.appendChild(imgElement);
    }else if(broj==9)
    {
        document.getElementById('glavni').style.display='none';
        document.getElementById('viewMore').style.display='flex';

        // 1. Kreiranje img elementa
        const imgElement = document.createElement('img');

        // 2. Postavljanje atributa
        imgElement.src = 'card9-photo.webp';
        imgElement.width = 550; // Širina u pikselima
        imgElement.id = 'dodati';

        // 3. Dohvatanje roditeljskog elementa
        const container = document.getElementById('slika');

        // 4. Dodavanje img elementa u DOM
        container.appendChild(imgElement);
    }else if(broj==10)
    {
        document.getElementById('glavni').style.display='none';
        document.getElementById('viewMore').style.display='flex';

        // 1. Kreiranje img elementa
        const imgElement = document.createElement('img');

        // 2. Postavljanje atributa
        imgElement.src = 'card10-photo.webp';
        imgElement.width = 550; // Širina u pikselima
        imgElement.id = 'dodati';

        // 3. Dohvatanje roditeljskog elementa
        const container = document.getElementById('slika');

        // 4. Dodavanje img elementa u DOM
        container.appendChild(imgElement);
    }
    else if(broj==11)
    {
        document.getElementById('glavni').style.display='none';
        document.getElementById('viewMore').style.display='flex';

        // 1. Kreiranje img elementa
        const imgElement = document.createElement('img');

        // 2. Postavljanje atributa
        imgElement.src = 'card11-photo.webp';
        imgElement.width = 550; // Širina u pikselima
        imgElement.id = 'dodati';

        // 3. Dohvatanje roditeljskog elementa
        const container = document.getElementById('slika');

        // 4. Dodavanje img elementa u DOM
        container.appendChild(imgElement);
    }
    else if(broj==12)
    {
        document.getElementById('glavni').style.display='none';
        document.getElementById('viewMore').style.display='flex';

        // 1. Kreiranje img elementa
        const imgElement = document.createElement('img');

        // 2. Postavljanje atributa
        imgElement.src = 'card12-photo.webp';
        imgElement.width = 550; // Širina u pikselima
        imgElement.id = 'dodati';

        // 3. Dohvatanje roditeljskog elementa
        const container = document.getElementById('slika');

        // 4. Dodavanje img elementa u DOM
        container.appendChild(imgElement);
    }
    else if(broj==13)
    {
        document.getElementById('glavni').style.display='none';
        document.getElementById('viewMore').style.display='flex';

        // 1. Kreiranje img elementa
        const imgElement = document.createElement('img');

        // 2. Postavljanje atributa
        imgElement.src = 'card13-photo.webp';
        imgElement.width = 550; // Širina u pikselima
        imgElement.id = 'dodati';

        // 3. Dohvatanje roditeljskog elementa
        const container = document.getElementById('slika');

        // 4. Dodavanje img elementa u DOM
        container.appendChild(imgElement);
    }
    else if(broj==14)
    {
        document.getElementById('glavni').style.display='none';
        document.getElementById('viewMore').style.display='flex';

        // 1. Kreiranje img elementa
        const imgElement = document.createElement('img');

        // 2. Postavljanje atributa
        imgElement.src = 'card14-photo.webp';
        imgElement.width = 550; // Širina u pikselima
        imgElement.id = 'dodati';

        // 3. Dohvatanje roditeljskog elementa
        const container = document.getElementById('slika');

        // 4. Dodavanje img elementa u DOM
        container.appendChild(imgElement);
    }
    else if(broj==15)
    {
        document.getElementById('glavni').style.display='none';
        document.getElementById('viewMore').style.display='flex';

        // 1. Kreiranje img elementa
        const imgElement = document.createElement('img');

        // 2. Postavljanje atributa
        imgElement.src = 'card15-photo.webp';
        imgElement.width = 550; // Širina u pikselima
        imgElement.id = 'dodati';

        // 3. Dohvatanje roditeljskog elementa
        const container = document.getElementById('slika');

        // 4. Dodavanje img elementa u DOM
        container.appendChild(imgElement);
    }
    else if(broj==16)
    {
        document.getElementById('glavni').style.display='none';
        document.getElementById('viewMore').style.display='flex';

        // 1. Kreiranje img elementa
        const imgElement = document.createElement('img');

        // 2. Postavljanje atributa
        imgElement.src = 'card16-photo.webp';
        imgElement.width = 550; // Širina u pikselima
        imgElement.id = 'dodati';

        // 3. Dohvatanje roditeljskog elementa
        const container = document.getElementById('slika');

        // 4. Dodavanje img elementa u DOM
        container.appendChild(imgElement);
    }
}

function smanjiSliku(){
    document.getElementById('glavni').style.display='block';
    document.getElementById('viewMore').style.display='none';
    document.getElementById('dodati').remove();
}

function klik(){
    toastr.success('Akcija je uspješno završena!', 'Uspjeh');
}

function prikazi(broj){
    document.getElementById('dodatak'+broj).style.display='block';
}

function sakrij(broj){
    document.getElementById('dodatak'+broj).style.display='none';
}



//prikazi json
//function prikaziJSON(){
    fetch('description.json')
    .then(response => {
        if (!response.ok) {
        throw new Error('Network response was not ok ' + response.statusText);
        }
        return response.json();
    })
    .then(data => {
        //console.log(data); // Prikazuje JSON podatke
        //console.log(data.description1); // Pristupa vrednosti ključa "name"
        //a=data;
        //console.log(a);
        downloadData(data);
        
    })
    .catch(error => {
        console.error('There has been a problem with your fetch operation:', error);
    });
//}

function downloadData(data){
    document.getElementById('description1').innerHTML=(data[0].description1);
    document.getElementById('prize1').innerHTML=(data[0].prize1)

    document.getElementById('description2').innerHTML=(data[1].description2);
    document.getElementById('prize2').innerHTML=(data[1].prize2)

    document.getElementById('description3').innerHTML=(data[2].description3);
    document.getElementById('prize3').innerHTML=(data[2].prize3)

    document.getElementById('description4').innerHTML=(data[3].description4);
    document.getElementById('prize4').innerHTML=(data[3].prize4)

    document.getElementById('description5').innerHTML=(data[4].description5);
    document.getElementById('prize5').innerHTML=(data[4].prize5)

    document.getElementById('description6').innerHTML=(data[5].description6);
    document.getElementById('prize6').innerHTML=(data[5].prize6)

    document.getElementById('description7').innerHTML=(data[6].description7);
    document.getElementById('prize7').innerHTML=(data[6].prize7)

    document.getElementById('description8').innerHTML=(data[7].description8);
    document.getElementById('prize8').innerHTML=(data[7].prize8)

    document.getElementById('description9').innerHTML=(data[8].description9);
    document.getElementById('prize9').innerHTML=(data[8].prize9)

    document.getElementById('description10').innerHTML=(data[9].description10);
    document.getElementById('prize10').innerHTML=(data[9].prize10)

    document.getElementById('description11').innerHTML=(data[10].description11);
    document.getElementById('prize11').innerHTML=(data[10].prize11)

    document.getElementById('description12').innerHTML=(data[11].description12);
    document.getElementById('prize12').innerHTML=(data[11].prize12)

    document.getElementById('description13').innerHTML=(data[12].description13);
    document.getElementById('prize13').innerHTML=(data[12].prize13)

    document.getElementById('description14').innerHTML=(data[13].description14);
    document.getElementById('prize14').innerHTML=(data[13].prize14)

    document.getElementById('description15').innerHTML=(data[14].description15);
    document.getElementById('prize15').innerHTML=(data[14].prize15)

    document.getElementById('description16').innerHTML=(data[15].description16);
    document.getElementById('prize16').innerHTML=(data[15].prize16)
}

function deleteData(divName){
    document.getElementById(divName).style.display='none';
}

function editData(editDiv){
    result=prompt('Unesi izmjenu: ')
    document.getElementById(editDiv).innerHTML=result;
}


function velicinaFonta(velFont){
    let arrayTranslate=document.getElementsByClassName('translate')
    
    for(let i=0;i<arrayTranslate.length;i++){
        arrayTranslate[i].style.fontSize=velFont+'px'
    }
}

velicinaFonta(16);

document.getElementById('accordion').style.display="none"
function accordion(){
    document.getElementById('accordion').style.display="block"
}

function accordionHide(){
    document.getElementById('accordion').style.display="none"
}